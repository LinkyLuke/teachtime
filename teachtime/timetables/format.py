class TimetableFormatter:
    """Given a user's timetable, formats it with the events contained within it."""

    def __init__(self, page):
        self.page = page

        # Load user's timetables from DB

    @property
    def next_page(self):
        return page += 1

    @property
    def previous_page(self):
        return page -= 1